%header.page-header
  .wrapper{role: 'layout'}
    .breadcrumbs
      .breadcrumbs_crumb= link_to "Admin", [:admin, :root]
      .breadcrumbs_crumb= link_to "Events", [:admin, :events]
      .breadcrumbs_crumb
        %h1 Election on #{ @election.occurred_on.strftime('%-d.%m.%y') }

.primary-main_content
  .wrapper{role: 'layout'}
    %section
    %main
      .box.-highlighted.-padded
        :markdown
          This is just for bug-hunting. Nothing editable.

      %section
        %header.subheader Election
        %main
          .text.-fixed
            #{ @election.inspect }