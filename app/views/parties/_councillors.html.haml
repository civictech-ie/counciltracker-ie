.view-content.-omega
  .wrapper{role: 'layout'}
    %ul.list.-enumerable
      - party.active_councillors.each do |councillor|
        %li
          = link_to councillor do
            = render partial: 'councillors/councillor', locals: {councillor: councillor}