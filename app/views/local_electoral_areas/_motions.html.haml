.view-content.-omega
  - if local_electoral_area.motions.any?
    .wrapper{role: 'layout'}
      %ul.list.-enumerable
        - local_electoral_area.motions.published.each do |motion|
          %li
            = link_to motion do
              = render partial: 'motions/motion', locals: {motion: motion}
  - else
    .wrapper{role: 'layout'}
      .empty-explanation
        %div
          %label None recorded