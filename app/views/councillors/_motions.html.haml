.view-content.-omega
  - if councillor.proposed_motions.published.any?
    .wrapper{role: 'layout'}
      %ul.list.-enumerable
        - councillor.proposed_motions.published.by_occurred_on.each do |motion|
          %li
            = link_to motion do
              = render partial: 'motions/motion', locals: {motion: motion}
  - else
    .wrapper{role: 'layout'}
      .empty-explanation
        %div
          %label None recorded