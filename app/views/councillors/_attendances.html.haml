.view-content.-omega
  .wrapper{role: 'layout'}
    %ul.list.-enumerable
      - councillor.attendances.countable.each do |attendance|
        %li
          = link_to attendance.attendable.path do
            = render partial: 'councillors/attendance', locals: {attendance: attendance}